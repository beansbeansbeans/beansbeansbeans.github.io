define([], function() {
    var renderer = {
        initialize: function(id) {
            // $("#log-" + id + " .log-contents").append('<canvas id="fft" width="350" height="300"></canvas><canvas id="letters" width="400" height="400"></canvas>');
        }
    }
    return renderer;
});
